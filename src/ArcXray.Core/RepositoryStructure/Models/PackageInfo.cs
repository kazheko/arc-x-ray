namespace ArcXray.Core.RepositoryStructure.Models
{
    /// <summary>
    /// Represents a NuGet package with its name and version.
    /// </summary>
    public record PackageInfo(string Name, string Version);
}
